# ============================================
# AI模型管理工具 + Web-to-API 环境变量配置
# ============================================

# 服务器端口
PORT=3000

# 数据库配置
DB_PATH=./ai_models.db

# API调用超时时间（毫秒）
API_TIMEOUT=10000

# ============================================
# 浏览器自动化配置 (Web-to-API)
# ============================================

# 浏览器端口 (Chrome远程调试端口)
BROWSER_PORT=9222

# 默认网站配置
# 设置此项后，服务启动时会自动打开指定的AI网站
# 可选值：aistudio.google.com, lmarena.ai, grok.com, chatgpt.com, chat.deepseek.com, www.doubao.com, claude.ai
DEFAULT_SITE=chatgpt.com

# 是否阻止资源加载（图片、样式等）以提升速度
BLOCK_RESOURCES=false

# 日志配置
LOG_LEVEL=info

# 是否开启浏览器功能
BROWSER_ENABLED=true

# ============================================
# 安全配置
# ============================================

# Cookie加密密钥 (必需)
# 用于加密存储敏感数据（如API密钥、Cookie等）
# 请使用以下命令生成一个安全的随机密钥：
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# 
# 示例生成命令（在项目根目录运行）：
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#
# ⚠️ 重要提示：
# 1. 请勿使用默认密钥或简单字符串
# 2. 密钥长度必须为64个十六进制字符（32字节）
# 3. 请妥善保管此密钥，丢失后无法解密已加密的数据
# 4. 在生产环境中，请确保此密钥的安全存储
# 5. 更换密钥后，之前加密的数据将无法解密
#
# 示例格式（请替换为您自己生成的密钥）：
COOKIE_ENCRYPTION_KEY=your-64-character-hex-key-here-replace-with-generated-key
